<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- 1. CORRECCIÓN: Cambiado de com.example a mx.edu.orderflow -->
    <groupId>mx.edu.orderflow</groupId>
    <!-- 2. CORRECCIÓN: Cambiado de PruebaProyectoGitHubActions a orderflow -->
    <artifactId>orderflow</artifactId>
    <!-- 3. CORRECCIÓN: Cambiado de 1.0-SNAPSHOT a 0.1.0-SNAPSHOT -->
    <version>0.1.0-SNAPSHOT</version>

    <!-- 4. CORRECCIÓN MANDATORIA: Declarar que es un proyecto contenedor/agregador -->
    <packaging>pom</packaging>

    <properties>
        <sonar.organization>manuel-romo</sonar.organization>
        <sonar.projectKey>mx.edu.orderflow:orderflow</sonar.projectKey>
        <sonar.host.url>https://sonarcloud.io</sonar.host.url>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- CORRECCIÓN: Agrega esta línea para definir la variable -->
        <junit.version>5.11.0</junit.version>
    </properties>

    <modules>
        <module>orders-api</module>
        <module>notifications-lambda</module>
    </modules>

    <build>
        <!-- ESTA NUEVA SECCIÓN FUERZA LA EJECUCIÓN EN TODOS LOS MÓDULOS -->
        <plugins>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>
        </plugins>

        <!-- Aquí se queda tu configuración existente -->
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <release>21</release>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.2</version>
                </plugin>
                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>0.8.12</version>
                    <executions>
                        <execution>
                            <id>prepare-agent</id>
                            <goals>
                                <goal>prepare-agent</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>report</id>
                            <phase>test</phase>
                            <goals>
                                <goal>report</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>